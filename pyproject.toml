[project]
name = "api-hub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manish M Naik", email = "mr.manishmnaik@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
api-hub = "api_hub:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
